# Sensitive Data
.env
current_principal.txt

# Data Files (User requested to hide CSVs)
*.csv
start_principal.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.vscode/
.idea/
.DS_Store

# Static build artifacts (optional, but usually we commit these for Pages if usage is simple)
# But if we want to run build locally and push the RESULT, we KEEP static/data/*.json
# We DO NOT ignore static/data/*.json because GitHub Pages needs them.
